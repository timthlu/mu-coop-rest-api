{
  "name": "lutimlu-rest-api",
  "version": "1.0.0",
  "description": "1. Clone this repo.\r 1. Create your own repo (`<lan-id>-rest-api`) and copy over all the files in this repo (`README.md` optional).Make sure the 2 'hidden' files like .`editorconfig` and `.gitignore` are also copied over.\r 1. Download the following:",
  "main": "data.js",
  "scripts": {
    "start": "node -r esm src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "esm": "^3.2.25",
    "express": "^4.18.1",
    "swagger-ui-express": "^4.5.0"
  }
}
